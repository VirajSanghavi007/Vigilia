"""Placeholder so `pytest tests/integration` doesn't exit 5 (no tests
collected) while the DB-backed API is being rebuilt. Delete once real
integration tests land (e.g. against infra/db once it exists)."""


def test_placeholder():
    assert True
