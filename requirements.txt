# Core API + demo serving (dashboard, investigate, case manager, whitelist).
# These run off the committed pipeline_cache.json — NO torch needed at boot.
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.4
slowapi
python-multipart
pandas==3.0.2
numpy==2.4.4
networkx==3.6.1
scikit-learn==1.8.0
openpyxl
psycopg2-binary
argon2-cffi

# ML deps (torch + torch_geometric) are installed separately by the Dockerfile
# for Hugging Face Spaces — kept out of here so the base install stays light.
